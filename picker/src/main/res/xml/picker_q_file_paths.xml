<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!--代表app 私有的存储区域 Context.getCacheDir()目录下的images目录 -->
    <!-- /data/user/0/你的包名/cache-->

    <!-- pictures path -->
    <files-path
        name="picker_pictures"
        path="Pictures"/>

    <!-- movies path -->
    <files-path
        name="picker_movies"
        path="Movies"/>

    <!-- thumbs path -->
    <cache-path
        name="picker_thumbs"
        path="VideoThumbs"/>
    <!--copy path-->
    <cache-path
        name="picker_copies"
        path="pickerImageCopy"/>

    <!-- pictures path -->
    <external-files-path
        name="picker_external_pictures"
        path="Pictures"/>


    <!-- movies path -->
    <external-files-path
        name="picker_external_movies"
        path="Movies"/>

    <!-- thumbs path -->
    <external-cache-path
        name="picker_external_thumbs"
        path="VideoThumbs"/>
    <!--copy path-->
    <external-cache-path
        name="picker_external_copies"
        path="pickerImageCopy"/>

</paths>